package io.zhudy.xim.session;

/** @author Kevin Zou (kevinz@weghst.com) */
public enum SessionEvent {
  /** 添加成功之前. */
  SM_ADD_BEFORE,
  /** 添加成功之后. */
  SM_ADD_AFTER,
  /** 移除成功之前. */
  SM_REMOVE_BEFORE,
  /** 移除成功之后. */
  SM_REMOVE_AFTER,
}
